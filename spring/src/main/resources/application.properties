### SPRING APPLICATION ###
spring.application.name=api-server
server.port=8443

### SPRING ACTUATOR & SECURITY ###
management.endpoints.enabled-by-default=false
management.endpoint.health.enabled=true
management.endpoints.web.exposure.include=health
spring.security.user.name = spring-admin

### DATABASE (focust_db) ###
spring.jpa.hibernate.ddl-auto = update
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://mysql/focust_db
spring.datasource.username = root

### SSL CERTIFICATE ###
#server.ssl.key-store=classpath:keystore/focust-spring.p12
#server.ssl.key-store-type=pkcs12
#server.ssl.alias=focust-spring

#==========================================================#
#    Below will contain lines generated by Docker, as      #
#    it would include sensitive information that should    #
#    not be leaked in the GitHub repository.               #
#==========================================================#